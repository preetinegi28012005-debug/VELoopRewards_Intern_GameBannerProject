/**
 * Wormzy entry point. The full 15-level playable game lives in ./wormzy and is
 * re-exported here so the existing /games/wormzy route keeps working unchanged.
 */
export { WormzyGame } from './wormzy/WormzyGame';
