/**
 * Aqua Fill entry point. The full 20-level drawing puzzle lives in ./aqua and
 * is re-exported here so the existing /games/aqua-fill route keeps working.
 */
export { AquaFillGame } from './aqua/AquaFillGame';
